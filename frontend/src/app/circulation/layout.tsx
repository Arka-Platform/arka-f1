import type { ReactNode } from 'react'

export const metadata = {
  title: 'Circulation · Arka',
  description: 'Browse books in circulation',
}

export default function CirculationLayout({ children }: { children: ReactNode }) {
  return children
}
