server:
  port: 8080
  shutdown: graceful

spring:
  application:
    name: arka-backend
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 10MB
  datasource:
    url: jdbc:h2:mem:arka;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password: sa
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
    show-sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration

app:
  cors:
    allowed-origins:
      - http://localhost:5173
  jwt:
    secret: ${JWT_SECRET:arka-secret-key-change-in-production-min-256-bits-long-please-use-a-strong-secret-key}
    expiration: ${JWT_EXPIRATION:86400000}  # 24 hours in milliseconds
  amazon-bestsellers:
    enabled: true  # Enable Amazon bestsellers CSV import
  aws:
    s3:
      bucket-name: ${AWS_S3_BUCKET_NAME:}
      region: ${AWS_REGION:us-east-1}

management:
  endpoints:
    web:
      exposure:
        include: "health,info,prometheus"
  endpoint:
    health:
      probes:
        enabled: true

logging:
  level:
    root: INFO
    org.springframework.security: WARN

---
# Production profile
spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/arka}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD}  # Must be set - no default to prevent empty password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration

app:
  cors:
    allowed-origins:
      - ${FRONTEND_URL:http://localhost:5173}
  amazon-bestsellers:
    enabled: true  # Enable in production - CSV files are in JAR resources
  aws:
    s3:
      bucket-name: ${AWS_S3_BUCKET_NAME:}
      region: ${AWS_REGION:us-east-1}

---
# Local development overrides
spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:h2:mem:arka_local;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
  jpa:
    hibernate:
      ddl-auto: create-drop  # Auto-create schema from entities for local dev
  flyway:
    enabled: false  # Disable Flyway for local development

app:
  cors:
    allowed-origins:
      - http://localhost:5173
      - http://localhost:3000
  amazon-bestsellers:
    enabled: true  # Enable for local development











